# 把传递依赖（如 ssh2-sftp-client 依赖的 ssh2 及其 asn1 等）扁平化到 node_modules 顶层，
# 避免 electron-builder 漏打包 .pnpm 内的嵌套依赖。
# node-linker=hoisted 是 pnpm v10+ 真正生效的扁平化开关（shamefully-hoist 的别名，
# 但 v10 起有时不读取，故直接用 hoisted）。
node-linker=hoisted
