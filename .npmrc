# Flatten node_modules so transitive deps (e.g. ssh2, required by
# ssh2-sftp-client) are hoisted to the top level and get packaged by
# electron-builder. Without this, electron-builder drops transitive deps
# that live only inside .pnpm/, causing "Cannot find module 'ssh2'".
shamefully-hoist=true
